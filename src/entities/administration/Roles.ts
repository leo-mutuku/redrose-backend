export class Role {
    constructor(
        public readonly role_id: number,
        public readonly role_name: string,
        public readonly role_code: number,
        public readonly description: string

    ) { }
}